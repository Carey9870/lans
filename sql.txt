1. Creating Dump ──(action㉿ACT)-[~/Desktop/lands.g]
└─$ sudo -u postgres pg_dump --no-owner --no-privileges -U postgres -d lands > lands_dump.sql


2. Pushing dump to neon database
    -> psql 'postgresql://neondb_owner:npg_gJfS9KMTO0Ek@ep-solitary-shape-ab4fu41p-pooler.eu-west-2.aws.neon.tech/neondb?sslmode=require&channel_binding=require' -f lands_dump.sql

    
task:
    1. sql - images and videos fetch via link
    3. how to send otp
    4. 

    ---

https://www.youtube.com/watch?v=1YgtDgA1sqI - link to jos selman


---
sudo -i -u postgres
psql
\c lands

sudo systemctl status postgresql
    or
sudo systemctl start postgresql

---

catch (error: unknown) {
  if (error instanceof Error) {
    return NextResponse.json({ error: error.message }, { status: 500 });
  }

  return NextResponse.json(
    { error: "Unknown error occurred" },
    { status: 500 }
  );
}


or

catch (error) {
  if (error instanceof Error) {
    return NextResponse.json({ error: error.message }, { status: 500 });
  }

  return NextResponse.json({ error: "Something went wrong" }, { status: 500 });
}


---------------------------

